/** @type {import('next-sitemap').IConfig} */

const config = {
    siteUrl: 'https://pk-feniks.com',
    generateRobotsTxt: true, // (optional)
    generateIndexSitemap: false
    // ...other options
}

module.exports = config;